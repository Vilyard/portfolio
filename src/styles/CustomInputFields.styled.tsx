import { TextField, TextareaAutosize, styled } from "@mui/material";

export const StyledInputField = styled(TextField)({
  margin: "0 auto",
  width: "80%",
});

export const StyledTextAutosize = styled(TextareaAutosize)({
  width: "80%",
  margin: "0 auto",
});

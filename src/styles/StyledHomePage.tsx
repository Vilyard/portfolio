import { Box, styled } from "@mui/material";

export const StyledHome = styled(Box)({
  display: "flex",
  flexDirection: "column",
  gap: 10,
  width: "100wv",
  height: "100wh",
  margin: "0 auto",
});
